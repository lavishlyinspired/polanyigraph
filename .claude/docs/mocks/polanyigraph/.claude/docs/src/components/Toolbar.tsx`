import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { Sparkles, Zap, Brain, RotateCcw } from 'lucide-react';

interface ToolbarProps {
  nodeCount: number;
  edgeCount: number;
  onRunInference: () => void;
  onClearActivation: () => void;
  onReset: () => void;
}

export function Toolbar({ nodeCount, edgeCount, onRunInference, onClearActivation, onReset }: ToolbarProps) {
  return (
    <div className="flex items-center justify-between px-6 py-3 border-b border-slate-200 bg-white/80 backdrop-blur-sm">
      <div className="flex items-center gap-3">
        <div className="flex items-center gap-2">
          <div className="w-8 h-8 rounded-lg bg-gradient-to-br from-amber-500 to-orange-600 flex items-center justify-center shadow-sm">
            <Brain className="w-5 h-5 text-white" />
          </div>
          <div>
            <h1 className="text-base font-serif font-bold text-slate-800 leading-none">
              NeuroGraph <span className="text-amber-600">OS</span>
            </h1>
            <p className="text-[10px] text-slate-400 leading-none mt-0.5">
              spreading activation + symbolic inference
            </p>
          </div>
        </div>
        <div className="flex items-center gap-2 ml-4">
          <Badge variant="secondary" className="bg-slate-100 text-slate-600 font-mono text-xs">
            {nodeCount} nodes
          </Badge>
          <Badge variant="secondary" className="bg-slate-100 text-slate-600 font-mono text-xs">
            {edgeCount} edges
          </Badge>
        </div>
      </div>
      <div className="flex items-center gap-2">
        <Button
          size="sm"
          variant="outline"
          className="border-slate-300 text-slate-600"
          onClick={onClearActivation}
        >
          <Sparkles className="w-3.5 h-3.5 mr-1" />
          Clear activation
        </Button>
        <Button
          size="sm"
          className="bg-cyan-700 hover:bg-cyan-800 text-white"
          onClick={onRunInference}
        >
          <Zap className="w-3.5 h-3.5 mr-1" />
          Run inference
        </Button>
        <Button
          size="sm"
          variant="ghost"
          className="text-slate-400"
          onClick={onReset}
        >
          <RotateCcw className="w-3.5 h-3.5" />
        </Button>
      </div>
    </div>
  );
}