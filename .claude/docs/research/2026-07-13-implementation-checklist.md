# Implementation Checklist

> Companion to `2026-07-13-rule-mining-and-compound-query-implementation-plan.md` — same features, ordered as a build sequence with checkboxes. Update this file's checkboxes as work lands; don't duplicate status in the plan doc itself.
>
> **Revision note (2nd)**: §10 ("Loop Engineering — analysis and verdict," the repo-maintenance Daily Triage idea) is **out of scope for this checklist** — not being built. Dropped entirely, including the `LOOP_STATE.md` state file. What stays in scope is §12, "Loop Engineering applied to the core agentic orchestration framework" — the part that actually touches this project's own KG business logic (Feature 7, the autonomous graph maintenance loop) and Feature 4's synthesis step (§12.4's grounding check). Those are represented explicitly below, not scattered into other phases.

## Phase 1 — De-risk the reasoning engine (no dependencies, do first)

- [x] **Feature 1: Rule Aggregation Policy** (plan §3) — **done**
  - [x] Two-pass restructure of `run_inference` in `reasoning/engine.py`
  - [x] `DerivedFact.supporting_rule_ids` + `contributing_fact_ids` fields
  - [x] Noisy-OR combination for multi-rule firings on the same edge
  - [x] `reason()`'s idempotency bookkeeping updated to track all contributing rules' fact ids, not just the merged fact's id (regression guard test added)
  - [x] Tests: aggregation (noisy-OR value + supporting/contributing ids), single-rule case unchanged, no re-firing across iterations — `tests/test_reasoning.py`, 14/14 passing including 3 new tests
- [x] **Feature 2: Semantic Conditioning at Inference** (plan §4) — **done**
  - [x] Confirmed the ontology loader had no domain/range check yet; added `OntologySchema.build_domain_range_matcher()` in `ontology/schema.py`, fail-open for properties the loaded ontology doesn't describe
  - [x] Added `domain_range_check` param (default permissive `_always_valid`) to both `run_inference` and `reason()` in `reasoning/engine.py`, new `"ontology domain/range violation: ..."` skip_reason
  - [x] Wired through `reasoning_service.run_reasoning` via `schema.build_domain_range_matcher()`
  - [x] Tests: `tests/test_ontology_subclass.py` (6 new: accepts correct typing, rejects wrong source/target, subclass-aware, fails open for unknown property, fails open with no properties loaded) + `tests/test_reasoning.py` (2 new: gate actually skips a rule that would otherwise fire, default behavior unchanged when not supplied) — 29/29 passing across both files
  - [x] Confirmed all files touched (`reasoning/engine.py`, `ontology/schema.py`, `services/reasoning_service.py`) and every test importing them collect cleanly (live Neo4j/GraphDB-dependent tests can't execute in this sandbox, but import/wiring is verified sound)

## Phase 2 — Rule mining (self-contained, no agent-layer risk)

- [x] **Feature 3: Rule Mining** (plan §5) — **done**
  - [x] `services/rule_mining_service.py`: `mine_candidates`, `list_candidates`, `approve_candidate`, `reject_candidate`
  - [x] `:CandidateRule` Neo4j schema (MERGE-safe deterministic id `cand-{edge_type}-{source_type}-{target_type}`)
  - [x] `approve_candidate` calls existing `rules_store.create_rule` — confirmed zero engine changes needed, verified end-to-end (mined+approved rule fires a real `DerivedFact` in `run_reasoning`)
  - [x] REST endpoints: `POST /rules/mine/{graph_id}`, `GET /rules/candidates`, `POST /rules/candidates/{id}/approve`, `POST /rules/candidates/{id}/reject` — added to existing `api/rules.py`
  - [x] Frontend: "Suggested Rules" section added to `components/pages/LogicPage.tsx` (the live-mounted Rules Manager — `ConstructionPanel.tsx` is a dead/unmounted duplicate, confirmed via `App.tsx`), wired through `graphStore.ts` + `lib/api.ts`. Live-verified in the browser end-to-end: mined a real seeded pattern (`organization -[has strategic partnership with]-> organization`, support=3, confidence=100%), approved it, confirmed it appeared in the Active Rules Registry as a CUSTOM rule and the rule count incremented — not just a static render
  - [x] Tests: `tests/test_rule_mining_service.py` (5/5: support/confidence math, seed-rule exclusion, too-small-graph → zero results, reject → not resurfaced, approve → fires in real reasoning) + `tests/test_api_rule_mining.py` (5/5 endpoint tests) — all passing against real Neo4j/GraphDB
  - [x] `evals/cases/rule-mining/case-01.json` — new `_run_rule_mining` runner registered in `evals/lib.py` (rule mining is deterministic Python over real edges, no LLM call, so the case seeds entities/edges directly rather than via `ingest_service`); passes via both direct `run_case` and the full `run_evals.py --skill rule-mining` harness

## Phase 3 — Skill-graph-driven selection (prerequisite for Feature 4)

- [x] **Feature 0, Stage A: symbolic selection** (plan §2) — **done**
  - [x] `_select_skill()` in `agents/graph.py` replaces the direct `_SKILL_BY_INTENT.get(intent)` call in `responder_node`; hardcoded map demoted to fallback. Deliberately falls back to `_SKILL_BY_INTENT.get(intent)` (can be `None` for extract/reason) rather than the plan sketch's `"kg-extraction"` default — preserves the exact prior no-op behavior for intents the map never covered (a strict improvement only, never a regression). `AgentState` gained `discovered_skill_scores: list[float]`, parallel to the existing `discovered_skills`
  - [x] Test: `test_stage_a_discovery_overrides_the_hardcoded_map_for_a_strongly_matching_text` — empirically-verified-first (checked real Lucene scores against the live skill graph before writing the assertion) divergence case: intent="query" (maps to kg-query) but text overwhelmingly matches polanyi-enrichment's description → polanyi-enrichment wins
  - [x] Test: `test_stage_a_composition_survives_a_genuinely_unreachable_neo4j` — mirrors `ResilientSkillDiscovery`'s existing degradation pattern; tests the `find_skills` → `_select_skill` composition directly rather than through a full `build_graph()` run (a dead Neo4j client fed through `build_graph` would also break `querier_node`'s unrelated real reads, which isn't what this guarantee is about)
  - [x] 5 additional pure-function unit tests for `_select_skill` (confidence floor, empty discovery, unmapped-intent no-regression, unmapped-intent strict-improvement) — 7/7 new tests passing, 40/40 across all affected files, no regressions
  - [x] Live-verified against the running server: all 6 router intents still resolve to a sensible skill (matches `_SKILL_BY_INTENT` exactly for ordinary phrasing, as expected — divergence only occurs with deliberately skewed text, per the acceptance criterion)
- [ ] **Feature 0, Stage C: ontology-constrained eligibility** (plan §2) — **deliberately skipped, revisit later**
  - Re-checked all 6 real `SKILL.md` descriptions directly: every one is differentiated by *task* ("Use when extracting...", "Use when visualizing..."), none by entity type. Stage C's premise (filter skill candidates by entity-type match) has no real basis in the current skill set — building it now means inventing an unused constraint. Revisit only once a genuinely entity-type-restricted skill exists to filter on.
- [ ] **Feature 0, Stage B: bandit** (plan §2) — **deliberately skipped, revisit later**
  - Technically buildable now (the plan's own test spec uses scripted/deterministic data, not live usage), but adds non-deterministic skill selection to production routing with zero real usage history yet to justify it (project hasn't shipped/received traffic). Wrong tradeoff right before a release. Revisit once there's real `record_usage` history across the 6 skills to actually learn from.

## Phase 4 — Compound-query answering, including §12's core-framework Loop Engineering additions

- [x] **Spike first**: throwaway 3-node LangGraph (`start → [Send(a), Send(b)] → join → end`) proving fan-out/fan-in + `Annotated[dict, operator.or_]` concurrent-write pattern works against the installed LangGraph version (plan §6.9) — **done**, confirmed clean against `langgraph==1.2.9`, no API surprises. Also spiked the mixed case the real router needs (same conditional-edge callback returning either a `Send` list or a plain string) — works correctly, single-intent path stays fully isolated from compound machinery. Scripts discarded (scratchpad, not committed).
- [x] **Feature 4: Compound-Query Answering** (plan §6) — **done**
  - [x] `Settings.enable_compound_queries` flag (default off)
  - [x] `AgentState`: `intents`, `partial_answers` (with `operator.or_` reducer), `combined_answer`
  - [x] Router: comma-list parsing, compound few-shot examples (flag-gated). Two safety guards beyond the plan's literal sketch, both found through careful review of the Send-dispatch mechanics rather than assumed: (1) flag off unconditionally truncates to a single intent even if the base-prompt LLM ever free-associates a stray comma — the plan's own "provably identical, not just probably fine" guarantee, enforced in code, not just by prompt wording; (2) `"extract"` anywhere in a parsed intent list also collapses to single-intent — extraction can't structurally be one of several parallel `Send` branches (reasoning/querying over a graph "extract" hasn't written to yet would race it), and without this guard a compound list like `"extract,reason"` would silently skip extraction entirely
  - [x] Dispatch via `Send`, eligibility check against Feature 0's selector (not a second hardcoded map) — `_is_eligible` deliberately runs discovery on the RAW turn text (not `_discovery_query`'s intent-phrase-boosted version) for secondary intents specifically: phrase-boosting would make an intent's own mapped skill win its own query almost tautologically, defeating the point of an independent check. Verified empirically (real Neo4j scores) before finalizing, not assumed. Primary intent is exempt from this check, trusted exactly as much as the pre-Feature-4 single-intent path already trusted it
  - [x] Specialist nodes (`reasoner`, `querier`, `enricher`, `memory_agent`) write `partial_answers`
  - [x] Conditional post-specialist routing (`combiner` vs `responder`)
  - [x] `combiner_node` (deterministic) — refactored to a module-level pure function (not a `build_graph` closure) specifically so it's independently testable per the plan's own test spec
- [x] **§12.4 — maker/checker grounding check** (plan §12.4, folded into Feature 4, not a separate feature number) — **done**: `responder_node`'s compound branch gets a deterministic `_find_ungrounded_claims` check + one bounded LLM retry if it finds an over-claim. Calibrated empirically against a real live-captured compound reply, not assumed: plain keyword-overlap-ratio flagged genuinely grounded paraphrase sentences just as readily as fabricated ones (natural connective language dilutes overlap either way), so the actual check flags sentences introducing a specific NOVEL token (multi-word proper-noun phrase or number) absent from `combined_answer` — zero false positives against the real reply, correctly caught 2/2 novel tokens in a deliberately fabricated one
  - [x] Test: a deliberately fabricated over-claiming `reply` triggers exactly one retry, not a loop (plus a companion test confirming a genuinely grounded reply triggers zero retries)
- [x] Tests: router parsing (single/compound/garbage/extract-collapse) — 8 tests; `combiner_node` unit tests (2); grounding-check unit + integration tests (4); full regression suite with flag off — 24/24 unchanged, bit-for-bit identical; integration tests with flag on against real compound queries (fan-out + reducer + combiner + grounding retry) — all passing against real Neo4j/GraphDB
- [x] Live-verified with a real LLM (not FakeLLM), flag on, via a standalone script (not the live dev server's process/config): real router LLM correctly classified a compound message as `reason,recall`, both specialists dispatched in parallel with real output, and the synthesized final reply explicitly referenced both specialists' contributions per the prompt instruction

### §12.1 / §12.2 — already true today, confirmed

- [x] **§12.1**: one-line pointer added to `reasoning/engine.py`'s module docstring — `reason()`'s convergence loop (spread → infer → feed back → check convergence → repeat) is this project's own instance of the "Loop Engineering" source's `/goal` primitive, independently built for fixpoint correctness, same shape
- [x] **§12.2**: confirmed for what's actually built today (enrichment's `:ImplicitFact` review + Feature 3's mined-rule candidate approval) — added a one-line pointer to `rule_mining_service.py`'s docstring tying its pending/approved/rejected status explicitly to the same "Human Gate" pattern enrichment already uses. Features 5/6 (Phase 5, not yet built) aren't covered by this confirmation — revisit their docs/UI copy for the same consistent framing once they exist

## Phase 5 — Independent add-ons (any order, after their stated deps)

- [x] **Feature 6: Embedding-based entity resolution** (plan §11.2) — **done**
  - [x] Dedup check wired into `services/ingest_service.py`, new `services/entity_resolution_service.py` (`:DuplicateCandidate` pending/approved/rejected, same Human Gate pattern as Features 3/enrichment) + 3 REST endpoints on `api/ingest.py` (`GET /ingest/{graph_id}/duplicates`, `POST .../confirm`, `POST .../reject`)
  - [x] **Corrected the plan's own suggested threshold empirically, not assumed**: "e.g. >0.92" plain cosine similarity over full summaries turned out to score a genuine duplicate pair ("Acme Corp"/"Acme Corporation") *lower* (~0.87) than a genuinely different pair ("Acme Preferred Stock"/"Acme Common Stock", ~0.88) — full-summary embeddings pick up shared sentence structure as much as shared entity identity, so no single cosine threshold separates the two cases. Fixed with a hybrid: vector index for cheap candidate retrieval only (`vector_search_service.find_similar_entities`, no strict cutoff), deterministic label-token Jaccard filter (after stripping legal-entity suffixes like Corp/AG/Ltd) as the actual precision filter — verified against 7 real calibration pairs, every one classified correctly
  - [x] **Second real gap found via live-verification with a real LLM (not the deterministic FakeLLM tests use)**: extraction assigned the *same* real company different-but-related FIBO subtypes across documents ("stock corporation" vs "corporation"), which an exact-string type filter silently excluded from comparison entirely. Fixed by threading `OntologySchema.build_subclass_matcher()` through from `ingest_service.py` (already loaded once for extraction, no extra GraphDB call) into `entity_resolution_service.check_for_duplicate`, checked in both directions
  - [x] **Fixed an unrelated but load-bearing bug found along the way**: `tests/test_vector_search_service.py`'s `graph_id` fixture had no teardown at all (unlike every other fixture in this codebase) — had leaked 54 `Entity` + 27 `ChatSession` + 680 orphaned `ChatMessage` nodes into the shared dev Neo4j across this whole session's test runs, degrading the vector index enough that fresh nodes stopped reliably surfacing in ANN search. This was silently causing 2 "pre-existing flaky" test failures dismissed earlier in this session as unrelated — they were a real, fixable bug the whole time. Fixed the fixture, found and fixed the identical missing-cleanup pattern in 8 more test files, cleaned up all leaked data with itemized user confirmation at each step (shared-database destructive action)
  - [x] Tests: plan's own spec (near-identical names flagged; genuinely different similar-looking names not flagged) plus same-type-required / cross-subtype-still-flagged / confirm+reject CRUD / reject-not-resurfaced — 7 in `test_entity_resolution_service.py`, 1 end-to-end wiring test in `test_ingest_service.py`, all against real Neo4j + real embedder
  - [x] Live-verified against the running server with a real LLM: two real ingests of "Acme Corp" / "Acme Corporation" correctly flagged (similarity 0.906), confirm endpoint correctly promoted it to "approved"
- [x] **Feature 5: Rule confidence evolves from human review** (plan §11.1) — **done**. Turned out NOT to actually depend on Feature 0 Stage B (which was deliberately skipped this round): the plan's own design text describes "a rolling-average update, structurally identical to record_skill_usage" — that's what `record_skill_usage` *already does today* (Stage B was the not-yet-built upgrade to that math), so nothing blocked building this against the real, current mechanism now
  - [x] `rules_store.update_rule_weight(neo4j, rule_id, *, outcome)` — rolling average over all recorded `:RuleReviewOutcome` nodes, same mechanism as the skill graph's `record_skill_usage`
  - [x] **Real design gap found and fixed**: seed rules (`data/rules/fibo_rules.json`) are read-only JSON, but this feature needs to update ANY rule's weight, including seed ones. Fixed `rules_store.load_all_rules` (and `api/rules.py`'s `GET /rules`, which had the identical gap) to treat a Neo4j-stored `:Rule` with the same id as a seed rule as an OVERRIDE, not a second duplicate entry — without this, a reviewed seed rule would fire twice in `run_inference` (stale seed weight + new Neo4j weight), corrupting Feature 1's noisy-OR aggregation
  - [x] `reasoning_service.review_derived_fact` orchestrates: look up the fact's rule, update its weight, mark the fact reviewed (`graph_service.set_derived_fact_review_status`, same Human Gate pattern as `:ImplicitFact`/`:CandidateRule`/`:DuplicateCandidate`) — confirmed `reasoning/engine.py` needed zero changes, exactly as the plan claimed
  - [x] REST endpoints on `api/reason.py`: `POST /reason/{graph_id}/facts/{fact_id}/confirm`, `.../reject`; `GET /reason/{graph_id}/facts` now includes `reviewStatus`
  - [x] Tests: plan's own spec (scripted confirm/reject sequence with a known 3:1 ratio converges to 0.75, deterministic) in `test_rules_store.py`, plus a seed-rule-override test (no duplication) and an end-to-end test proving a review outcome changes a *later, real* `run_reasoning` call's output with zero engine changes — 5 new unit/integration tests + 4 new API tests, all passing
  - [x] Live-verified against the running server: rejected a real derived fact via REST, confirmed the real seed rule's weight moved 1.0→0.0, confirmed `GET /rules` still shows exactly one entry tagged `source: "seed"` (no duplicate). **Caught and fixed a real mistake from this same live-verification**: rules are global, not graph-scoped state — the weight override briefly leaked into the shared dev database after the verification and broke an unrelated test (`test_reason_intent_runs_reasoning_without_a_preceding_extraction`) until reverted
  - Frontend UI intentionally not built this round (plan mentions reusing enrichment's approve/reject component) — backend + REST are complete and live-verified; add the UI affordance when there's a natural pass through the Reason tab, not required to consider this feature functionally done

## Phase 6 — Autonomous graph maintenance loop (plan §12.3 — hard dependency on 3, 5, 6; cannot move earlier)

- [x] **Feature 7: Autonomous Graph Maintenance Loop** (plan §12.3) — **done**
  - [x] `services/graph_maintenance_loop.py` orchestrating mine (Feature 3) → reason to convergence → dedup-check backstop (Feature 6) → report current rule weights. **Step 4 corrected from the plan's own framing, not assumed**: the plan describes "Feature 5's `update_rule_weight` pass over any `:DerivedFact`s confirmed/rejected since the last run" (implying deferred batch work), but this project's actual Feature 5 implementation updates weight SYNCHRONOUSLY the moment a human reviews a fact (`reasoning_service.review_derived_fact`) — so there's no backlog for a batch step to process. Step 4 reports the current weight of whatever rules fired this run instead of inventing redundant recomputation
  - [x] State: **`:LoopRun` Neo4j nodes** (decided, not left ambiguous) — consistent with every other piece of review/audit state this project tracks (`:ImplicitFact`, `:CandidateRule`, `:DuplicateCandidate`, `:RuleReviewOutcome` all live in Neo4j, none in a flat file), queryable via REST, testable with the real-Neo4j-no-mocks convention a markdown file can't support
  - [x] **`mcp__scheduled-tasks__create_scheduled_task` — checked, does not exist**: grepped the entire backend for any cron/scheduler/task-queue infrastructure, found none. Rather than inventing a fake integration, built the real, callable boundary instead — `POST /graph-maintenance/{graph_id}/run` (+ `GET .../runs`) — that an actual deployment's own scheduler (cron, task queue, etc.) would hit on a cadence. Stated explicitly in the module docstring as a real, open deployment decision, not silently glossed over
  - [x] **L1-safe-by-construction claim: confirmed, with one honest nuance found and documented, not blindly agreed**: mining (step 1) and entity resolution (step 3) are genuinely safe by construction — both only ever create a candidate record awaiting human approval. Reasoning (step 2) is more nuanced: `:DerivedFact` creation was ALREADY gate-free *before* this feature existed (`chat_service.py` reads every derived fact for chat grounding regardless of review status — confirmed by reading the code, not assumed) — Feature 7 doesn't make this riskier, it just runs the same already-accepted `run_reasoning` operation on a schedule instead of on-demand. No NEW auto-apply behavior introduced
  - [x] Tests: all 3 real maker steps exercised together in one connected-component test graph (mining pattern + real seed-rule edge + an ingest-bypassing near-duplicate pair), an empty-graph graceful-skip test, an idempotency test (re-running never duplicates a candidate), plus 2 API tests — 5 new tests, all against real Neo4j/GraphDB/embedder
  - [x] Live-verified against the running server: real `POST /graph-maintenance/{graph_id}/run` fired real reasoning (1 new fact, converged in 2 iterations) and reported the real active rule weight, persisted and listable via `GET .../runs`

---

## Phase 7 — Follow-up work requested after the checklist above was completed (2026-07-13)

- [x] **Feature 5 UI**: Confirm/Reject buttons + review-status badge added to the Inference Lab's "Derived Triple Store" cards (`InferencePage.tsx`), wired to the already-existing backend REST endpoints. Live-verified in the browser with a real derived fact: clicked Confirm, badge changed to "CONFIRMED", rule weight updated on the backend (1.0 → 1.0, i.e. 1/1 confirmed) — then reverted (rules are global state, same standing precedent as earlier live-verification cleanups)
- [x] **Scheduler wiring — "let it run automatically"**: added `apscheduler` (new dependency; in-process `AsyncIOScheduler`, no external service needed for a single-process desktop app, same reasoning as LangGraph's `InMemorySaver` choice). `services/maintenance_schedule_service.py` (persisted per-graph `:MaintenanceSchedule` config, source of truth) + `services/maintenance_scheduler.py` (live job registry, resyncs from Neo4j at process startup so a restart doesn't lose an active schedule). **OFF by default per graph** (your explicit direction: real, automatic LLM/embedding spend needs opt-in, not opt-out). Found and fixed a real APScheduler footgun: `add_job(..., replace_existing=True)` silently fails to update an existing job's trigger unless the scheduler is already running — fixed with explicit remove-then-add instead of trusting that flag
- [x] **Scheduler configuration from the UI**: `GET/POST /graph-maintenance/{graph_id}/schedule` + a new "Auto" tab in the sidebar Tools panel (`GraphMaintenancePanel.tsx`) — toggle, interval (5min floor), "Run Now", and real run history. Live-verified in the browser: toggled a real schedule on for the `default` graph, confirmed it persisted via REST, ran "Run Now" against real data and saw a genuine run-history entry — then disabled it again (off-by-default is the resting state)
- [x] **Orchestrator "loop automatically" — generalized §12.4's maker/checker grounding check from the "compound" branch to every responder reply**: the intent-specific `user` prompt already holds the real grounding data for every branch (query results, derived facts, entities, memory hits, or compound's combined sub-answers), so the same `_find_ungrounded_claims` + one-bounded-retry check now applies universally with no new plumbing — removed the `if intent == "compound":` gate entirely. New test proves this against a plain "reason" intent (not compound): a fabricated reply triggers exactly one retry, same as compound already did. Live-verified against the running server with a real LLM across two different intents (extract, query) — both produced clean, grounded replies
- [x] **Phase 3 Stage B/C, reconsidered given the new scheduler — still deferred, decision unchanged**: the scheduler is off by default and even once enabled needs real *accumulated* usage history over calendar time, not something the scheduler's mere existence provides at implementation time (Stage B). Stage C's blocker (no entity-type-restricted skill exists in the current 6) is unrelated to scheduling and unchanged. Both remain documented, deliberate non-actions, not silently dropped

---

**Not on this checklist, by design** (plan §8/§11.5 Non-goals, plus §10 dropped per your instruction): PyTorch/PyTorch Geometric, GPU training, FB15K-237/NELL-995 benchmarking, full ExpressGNN/variational EM, MAGNet's literal RL machinery, temporal reasoning (flagged open in plan §11.3, deliberately deferred to its own future plan), and §10's repo-maintenance Daily Triage loop / `LOOP_STATE.md`.

**Documented but deliberately not scheduled**: plan §13, an R-GCN-based GNN reference design for salience/edge-weight augmentation and link-prediction-based rule mining — real architecture spec worth keeping, gated behind Feature 5's bandit proving insufficient on real production confirm/reject volume first. Nothing in §13 has a checklist item above; revisit §13 directly if that gate is ever met.
